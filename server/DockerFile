# Backend Dockerfile

# Use the official Node.js image (using the lightweight Alpine image to keep the docker image small)
FROM node:18-alpine

# Set the working directory inside the container
WORKDIR /app

# Copy over the package files into the container
COPY package*.json ./

# Install all the dependencies inside the container
RUN npm install

# Copy the rest of the backend code into the container
COPY . .

# Expose the port the app/backend will listen on
EXPOSE 3000

# Define the default command to start the backend server
CMD ["npm", "start"]